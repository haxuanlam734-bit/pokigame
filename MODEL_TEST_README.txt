MODEL TEST

- Main HQ uses src/assets/models/model_game_ready.glb and replaces the old procedural HQ.
- base_hq.glb is placed as a secondary tent/outpost southwest of the HQ.
- GLTFLoader is included in index.html.
- Run with Live Server. Check F12 Console for GLTFLoader/404 errors.
